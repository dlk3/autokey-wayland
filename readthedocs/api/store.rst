store API
=========

These API methods are available in all environments.

This class is invoked as the "store" class in AutoKey scripts.  For example, the
"autokey.model.store.Store.get_global_value()" method documented below is
called as "store.get_global_value()" in an AutoKey script.

.. automodule:: autokey.model.store
   :no-members:
.. autoclass:: Store
   :members:

